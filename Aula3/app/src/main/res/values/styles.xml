<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="editTextStyle">@style/EditTextStyle</item>
        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>

    <style name="EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/edit_text</item>
        <item name="android:textColor">@color/editTextTextColor</item>
        <item name="android:textColorHint">@color/editTextTextColor</item>
        <item name="android:textSize">@dimen/editTextTextSize</item>
        <item name="android:padding">@dimen/editTextPadding</item>

        <!-- "Layout" items are not being applied through application style. -->
        <!-- The style has to be applied directly on item to work. -->
        <item name="android:layout_marginTop">@dimen/editTextMarginTop</item>
        <item name="android:layout_marginBottom">@dimen/editTextMarginBottom</item>
        <item name="android:layout_margin">@dimen/editTextMarginBottom</item>
        <item name="android:layout_height">@dimen/editTextHeight</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:textSize">@dimen/buttonTextSize</item>
        <item name="android:padding">@dimen/buttonPadding</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
