<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="editTextStyle">@style/edit_text_style</item>
        <item name="buttonStyle">@style/action_button_style</item>
    </style>

    <style name="edit_text_style" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/edit_text</item>
        <item name="android:textColor">@color/edittext_text_color</item>
        <item name="android:textColorHint">@color/edittext_text_color</item>
        <item name="android:textSize">22sp</item>

        <!-- "Layout" items are not being applied through application style. -->
        <!-- We have to apply the style directly on item to work. -->
        <item name="android:layout_marginTop">@dimen/edit_text_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/edit_text_margin_bottom</item>
        <item name="android:layout_margin">@dimen/edit_text_margin_bottom</item>
        <item name="android:layout_height">@dimen/edit_text_height</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="action_button_style">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/action_button_text_color</item>
        <item name="android:padding">@dimen/action_button_padding</item>
    </style>

</resources>
